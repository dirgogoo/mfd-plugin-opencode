/**
 * Construct detail page (Level 3 — Individual Construct)
 * Shows full detail of a single v2 construct with relationship mini-graph.
 *
 * Supports: concept, enum, capability, invariant, property, objective.
 */
import type { ModelSnapshot } from "../types.js";
import type { ConstructType } from "../types.js";
export declare function renderConstructDetail(snapshot: ModelSnapshot, domainName: string, type: ConstructType, itemName: string): string;
//# sourceMappingURL=construct-detail.d.ts.map